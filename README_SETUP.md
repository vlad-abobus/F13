# F13 - Інструкція з налаштування

## 🚀 Швидкий старт

### 1. Встановлення залежностей

**Backend (Python):**
```bash
pip install -r requirements.txt
```

**Frontend (Node.js):**
```bash
cd client
npm install
```

### 2. Налаштування .env

Створіть файл `.env` в корені проекту (скопіюйте з `.env.example`):

```env
# Database (для development можна не вказувати DB_PASSWORD - використається SQLite)
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=freedom13

# JWT
JWT_SECRET_KEY=your-secret-key-change-in-production

# Server
SECRET_KEY=your-secret-key-too
FLASK_ENV=development
PORT=5000
HOST=127.0.0.1

# CORS
CORS_ORIGINS=http://localhost:3000

# Redis (опціонально)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
```

### 3. Ініціалізація бази даних

База даних створиться автоматично при першому запуску. Або вручну:

```bash
python -c "from app import create_app; from config import Config; app = create_app(Config); app.app_context().push(); from app.database import init_db; init_db()"
```

### 4. Запуск

**Development режим (окремо):**

Terminal 1 - Flask:
```bash
python run.py
```

Terminal 2 - React:
```bash
cd client
npm run dev
```

**Production режим:**

Спочатку зібрати фронтенд:
```bash
cd client
npm run build
```

Потім запустити Flask:
```bash
python run.py
```

### 5. Доступ

- **Frontend:** http://localhost:3000
- **API:** http://localhost:5000/api
- **Health Check:** http://localhost:5000/api/health

## 📁 Структура проекту

```
F13R/
├── app/                    # Flask backend
│   ├── models/            # SQLAlchemy моделі
│   ├── routes/            # API маршрути
│   ├── services/          # Бізнес-логіка
│   ├── middleware/        # Middleware (auth, captcha)
│   ├── utils/             # Утиліти
│   └── __init__.py        # Application Factory
├── client/                # React frontend
│   ├── src/
│   │   ├── pages/         # Сторінки
│   │   ├── components/    # Компоненти
│   │   ├── api/           # API клієнт
│   │   ├── store/         # Zustand store
│   │   └── contexts/      # React контексти
│   └── public/            # Статичні файли
├── ruffle/                # Ruffle Flash плеєр
├── games/                 # SWF файли ігор
├── MikuGPT_ver_1.0/      # MikuGPT AI
│   └── emotions/          # Зображення емоцій
├── uploads/               # Завантажені файли
├── config.py              # Конфігурація
├── run.py                 # Точка входу
└── requirements.txt       # Python залежності
```

## 🔧 Основні функції

- ✅ Реєстрація та вхід (з CAPTCHA)
- ✅ Створення постів з зображеннями
- ✅ Коментарі та лайки
- ✅ Профілі користувачів
- ✅ MikuGPT AI чат-бот
- ✅ Flash ігри через Ruffle
- ✅ Галерея зображень
- ✅ GoonZone (опитування, новини)
- ✅ Адмін панель
- ✅ Багатомовність (ru, uk, kz, en)

## 📝 Наступні кроки

1. Налаштуйте PostgreSQL для production
2. Додайте реальну CAPTCHA інтеграцію
3. Налаштуйте Redis для кешування
4. Додайте тести
5. Налаштуйте CI/CD

## 🐛 Вирішення проблем

**Помилка підключення до БД:**
- Перевірте налаштування в `.env`
- Для SQLite переконайтеся, що папка `instance/` існує

**Помилка CORS:**
- Перевірте `CORS_ORIGINS` в `.env`
- Переконайтеся, що фронтенд працює на правильному порту

**Ruffle не завантажується:**
- Перевірте, що файли в `ruffle/` доступні
- Перевірте консоль браузера на помилки

## 📚 Документація

Повна документація в `README.md`
