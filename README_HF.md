
## Hugging Face Spaces — інструкція розгортання

### Що це?

`app.py` — Gradio обгортка, що:
- ✅ Запускає Flask бекенд у фоновому потоці
- ✅ Показує повний веб-сайт Freedom13 у вкладці **"Сайт"**
- ✅ Має вкладку **"Чатбот"** з MikuGPT (з підтримкою Gemini API)

### Кроки розгортання

#### 1. Створіть Space на Hugging Face

Перейдіть на: https://huggingface.co/spaces/create

**Параметри:**
- **Repository name:** `freedom13` (або своє ім'я)
- **Space SDK:** `Gradio`
- **SDK Version:** `3.53.1` (або новіше)
- **License:** Apache 2.0

#### 2. Додайте файли до Space

**Варіант A: Вручну через web UI (найпростіше)**

Створіть файл `app.py`:
```bash
# Скопіюйте вміст https://raw.githubusercontent.com/vlad-abobus/F13/feature/gradio-iframe-full-backend/app.py
```

Створіть файл `requirements.txt`:
```
gradio>=3.50.0
google-genai>=0.3.0
flask>=3.0.0
flask-cors>=4.0.0
```

#### Варіант B: Клонуйте Space локально

```bash
git clone https://huggingface.co/spaces/YOUR_USERNAME/freedom13
cd freedom13

# Скопіюйте з основного репо
cp ../F13/app.py .
cp ../F13/space_requirements.txt requirements.txt

# Commit & push
git add .
git commit -m "Add Freedom13 Gradio app with iframe"
git push
```

#### 3. Додайте Secrets у Settings

Settings → Secrets and variables:

| Name | Value |
|------|-------|
| `GOOGLE_API_KEY` | `sk-...` (ваш Google GenAI API ключ) |

#### 4. Налаштуйте Hardware (опціонально)

Для запуску **повного бекенду** з БД та Redis:

- Settings → Hardware → `CPU upgrade` або більш потужний
- Secrets → додайте:
  - `START_FULL_BACKEND=true`
  - `DATABASE_URL=postgresql://...` (якщо PostgreSQL)

#### 5. Готово! 🚀

Hugging Face автоматично запустить Gradio. Ваш Space буде доступний за адресою:
```
https://huggingface.co/spaces/YOUR_USERNAME/freedom13
```

---

### Локальне тестування

```bash
# Встановіть мінімальні залежності
pip install -r space_requirements.txt

# Запустіть
python app.py

# Браузер відкриється на http://127.0.0.1:7860
```

- **Вкладка "Сайт":** показує iframe с Flask бекенду (+ React фронтенд якщо є `client/dist`)
- **Вкладка "Чатбот":** MikuGPT чатбот (потребує `GOOGLE_API_KEY`)

---

### Налаштування

| Змінна | Значення | За замовчуванням |
|--------|----------|-----------------|
| `GOOGLE_API_KEY` | Google GenAI API ключ | - |
| `START_FULL_BACKEND` | `true` для запуску всього стеку | `false` (лише Gradio) |
| `FLASK_PORT` | Порт for Flask бекенду | `5000` |
| `PORT` | Альтернатива для Flask порту | `5000` |

---

### Проблеми та рішення

**Q: "Вкладка 'Сайт' показує 404"**  
A: Flask бекенд не запustився. Перевірте:
- Чи має `client/dist` скомпільований фронтенд?
- Чи вибрано достатньо потужний Hardware?
- Чи налаштована БД (якщо потрібна)?

**Q: "Чатбот каже ERROR: GOOGLE_API_KEY не встановлено"**  
A: Додайте SECRET в Settings of Space:
- Name: `GOOGLE_API_KEY`
- Value: ваш API-ключ від Google

**Q: "Flask не запускається або падає"**  
A: По умовчанию `SKIP_INIT_DB=1`. Якщо потребуєте БД:
- Встановіть `START_FULL_BACKEND=true`
- Налаштуйте `DATABASE_URL` та Redis (якщо потрібен)

---

### Документація

- Повна інструкція запуску: [START_GUIDE.md](../START_GUIDE.md)
- Архітектура проекту: [README.md](../README.md)
- Gemini интеграция: [GEMINI_INTEGRATION_GUIDE.md](../GEMINI_INTEGRATION_GUIDE.md)
